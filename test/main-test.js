const {describe, it} = require('node:test');
const {strictEqual} = require('assert');

describe('Complex Numbers', function() {
  it('should get the real part of a complex number', function() {
    strictEqual(1, 1);
  });
});
